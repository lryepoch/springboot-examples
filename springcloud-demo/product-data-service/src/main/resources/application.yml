#server:
#  port: 8004 #因为会启动多个product-data-service，所以端口号由用户自动设置，推荐8001,8002,8003

euerka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: product-data-service
  zipkin:
    base-url: http://localhost:9411

